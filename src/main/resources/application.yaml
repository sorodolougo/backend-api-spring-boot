spring:
  application:
    name: backend-api-spring-boot

  datasource:
    url: jdbc:postgresql://localhost:5432/gescom_db
    username: postgres
    password: MySecurePassword2026
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
